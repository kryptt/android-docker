# ==============================================================================
# Device registry — one line per device
# ==============================================================================
# Format: device|serial|codename|platform|product|has_recovery|has_super|nfs_version|hostname|wifi_ip|k3s_node_ip
#
#   device:       Human name used in build.sh/setup.sh (e.g. oneplus7pro)
#   serial:       adb/fastboot serial number
#   codename:     LineageOS codename (for lineageos/ directory)
#   platform:     SoC directory under kernel/ (e.g. sm8150)
#   product:      fastboot product string for verification
#   has_recovery: true if device has separate recovery partition (SM8250+)
#   has_super:    true if device uses dynamic partitions (SM8250+)
#   nfs_version:  NFS version to use (3 or 4; use 3 for kernel < 4.19)
#   hostname:     Network hostname for the device
#   wifi_ip:      Static WiFi IP (the underlay address phones reach the LAN on)
#   k3s_node_ip:  Secondary IP added to wlan0 used as k3s --node-ip (dual-IP scheme).
#                 Lives in the cluster's pod-network subnet (10.20.0.0/24).
#                 Reachable via static routes on each Gentoo node:
#                     ip route add <k3s_node_ip>/32 via <wifi_ip>
#                 Set to '-' for NFS-only devices (no k3s).

oneplus7pro|2e2e5cbf|guacamole|sm8150|msmnile|false|false|3|hr-op7|192.168.2.24|10.20.0.24
oneplus8pro|8e1cdcbe|instantnoodlep|sm8250|kona|true|true|3|hr-op8|192.168.2.25|10.20.0.25
oneplus10pro|8e81497e|wly|sm8450|taro|true|true|3|hr-op10|192.168.2.113|-
